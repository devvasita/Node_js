/**
 * Template Engine helps to create an HTML template with minimal code. Also in can inject data into an HTML template
 *
 * at run time => template engine replace variables in a template file with actual values
 *  ==> to html and send to the client
 *
 *
 *
 */

// Popular Template Engines :

//  Ejs : Eembedded js template use for lightweight solution , creating HTML markup with simple

// HandleBars : Improve developer's productivity

// Mustache : mustache is logic less templating engine it can literally used for any kinf of devlopment

// Pug : pug is 2x slower than handlebars , EJS underscore
