// express.Router() : express.Router() function which is used to create a new router object.

// This function is used when you want to create a new a router object.
// So code cannot looks messy

// Syntax : express.Router()

// Router Instance is middleware.

// --------------- Commonjs module system and us6 moudle system : ---------------------------

// Node.js's original moudle system is commonJs (which use require and moudle.exports)

// ECAMAScript module system (which uses import and export) has become standard Node.js has added support for it.

// Node.js will treat .cjs files as CommonJS modules and .mjs files are ES6 modules.

// it will treat .js files at whatever the default modules system for the project . or we have to mention in package.json ( "type" : "module" )
